project.ext {
    def versionMajor = 0
    def versionMinor = 0
    def versionBuild = 3 // bump for dogfood builds, public betas, etc.
    globalVersionCode = versionMajor * 1000 + versionMinor * 100 + versionBuild
    globalVersionName = "${versionMajor}.${versionMinor}.${versionBuild}"
}
