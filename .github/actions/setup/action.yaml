runs:
  using: composite
  steps:
    - name: Setup JDK 17
      uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5
      with:
        distribution: temurin
        java-version: 17

    - name: Setup Android SDK
      uses: android-actions/setup-android@40fd30fb8d7440372e1316f5d1809ec01dcd3699 # v4

    - name: Cache Gradle
      uses: actions/cache@6f8efc29b200d32929f49075959781ed54ec270c # v3
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper/dists
        key: gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: gradle-

    - name: Setup local variables
      shell: bash
      run: |
        echo "VGS_TENANT_ID=$VGS_TENANT_ID" >> local.properties
        echo "VGS_PATH=$VGS_PATH" >> local.properties
        echo "VGS_TOKENIZATION_ROUTE_ID=$VGS_TOKENIZATION_ROUTE_ID" >> local.properties
        echo "VGS_TOKENIZATION_V2_ROUTE_ID=$VGS_TOKENIZATION_V2_ROUTE_ID" >> local.properties
        echo "VGS_ACCESS_TOKEN_URL=$VGS_ACCESS_TOKEN_URL" >> local.properties
        echo "VGS_CLIENT_ID=$VGS_CLIENT_ID" >> local.properties
        echo "VGS_CLIENT_SECRET=$VGS_CLIENT_SECRET" >> local.properties
        echo "VGS_GRANT_TYPE=$VGS_GRANT_TYPE" >> local.properties